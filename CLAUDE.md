# CLAUDE.md — 项目宪法（每个 session 自动加载，最先读这份）

> 你（AI）接手本项目时，**先读本文件，再读 `docs/PROGRESS.md` 最新一条看当前进度**。
> 本文件定义"我们怎么合作"和"不可违背的规矩"。它是元规则，优先级高于你的默认习惯。

---

## 〇、接手流程与文档加载规则（先读这条，省上下文）

文档按"加载层级"分三档，**不要一次读全部**，按需加载：

- **🟢 第 0 层·必读（极短）**：本文件 `CLAUDE.md` + `docs/PROGRESS.md` 的**最新一条**。读完即可开工。
- **🟡 第 1 层·开工前扫一遍**：`docs/ARCHITECTURE.md`，建立全局观，之后不必反复读。
- **🔵 第 2 层·按需查阅（动到才读）**：`HOOKS.md` / `MESSAGE_PROTOCOL.md` / `MODULES.md` / `PROVIDER.md`——动哪个模块读哪份。`DECISIONS.md` 仅当"想做的事与现有设计冲突"时查，看是否早有定论。各模块目录下的 `CHEATSHEET.md` 仅在实现该模块、owner 遇到技术盲区时查。

一句话：**必读只有 CLAUDE + PROGRESS 最新条；架构图开工扫一遍；其余按需，动到哪读哪。**

---

## 〇·补 文档写入规则（读规则的另一半，防止文档腐烂）

**第一原则——先分清"改"还是"追加"**：
- **活文档**（ARCHITECTURE / HOOKS / MESSAGE_PROTOCOL / MODULES / PROVIDER / ROADMAP）→ **只能改**，覆盖旧内容，始终反映当前真相。
- **账本文档**（DECISIONS / PROGRESS）→ **只能追加**，永不修改旧条目。

**什么时候必须写**（靠触发时机，不靠自觉）：

| 触发时机 | 写操作 |
|---------|--------|
| 做了改变架构约定的决定 | 追加 DECISIONS 一条 + 改对应活文档 + commit（决策#3 三件套） |
| 一个模块/一次工作收尾 | 追加 PROGRESS 一条（现在在哪+下一步）+ commit |
| 代码改动使某活文档过时 | 立即改那份活文档（代码与文档不许脱节） |
| 新开一个代码模块 | 建该模块的 `CHEATSHEET.md` |
| 仅改错别字/小调整 | 只 commit，不碰 DECISIONS/PROGRESS |

**两条硬约束**：
1. **代码与其对应文档在同一个 commit 落地**——不许"代码变了、文档还停在旧版"。
2. 改活文档时**同步更新其顶部的 `状态 / 对齐决策编号 / 最后更新`**，让人一眼看出新旧与同步关系。

**兜底判断**：拿不准要不要写文档时问自己——"三天后回来的人或另一个 AI 不知道这件事，会不会走弯路或重复讨论？" 会→写；不会→只 commit。

---

## 一、这是什么项目

一个**用于学习 agent 底层逻辑的、生产级的、可插拔模块化 agent 框架**。
目标不是完成某个业务任务，而是**看清 agent 的底层逻辑、覆盖尽可能全的能力模块、锻炼解决问题的能力**。

核心设计信条（来自 `docs/ARCHITECTURE.md`）：

> **健壮 = 极小且稳定的内核 + 想得极全的钩子点 + 数量不限的可插拔模块。**

---

## 二、和 owner 的协作契约（必须遵守）

owner 是项目负责人，编程基础不错但 agent 领域是学习者。合作方式是**"教练 + 陪练"**，不是"我全写完给他"：

1. **代码分工：AB 混合，随难度浮动。**
   - A：我给接口签名 + 类型 + docstring + `# TODO`，核心逻辑由 owner 填。
   - B：复杂模块先一起敲定设计，owner 独立实现，我做 code review。
   - owner 说"这个直接给我写完"时才整段代出完整实现。
2. **剧透尺度：架构级的坑我提前讲；实现级的小坑让 owner 自己踩**（踩坑是学习的一部分），卡住来问我再给思路。
3. **我不全程盯着。** owner 卡住会来问、模块写完会来找我审。我不主动逐行打断。
4. **发现问题**：架构级问题立刻提；实现级小问题等 owner 写完这段再一起看。
5. **诚实优先**：该泼冷水就泼，不吹捧。指出漏洞、权衡取舍，给建议而非讨好。
6. **技术盲区速查**：每个代码模块开工时，我先产出一份 `<模块目录>/CHEATSHEET.md`，**只收录该模块实际用到、且 owner 可能不熟的技术点**（每点：一句用途 + 最小例子 + 它在本模块哪处出现）。它是"陪练参考卡"，不是 Python 教程，owner 熟的不写。

---

## 三、文档治理规则（决策 #3，必须遵守）

文档分两类：

- **活文档**（描述系统现状，可更新覆盖）：`ARCHITECTURE.md`、`HOOKS.md`、`MESSAGE_PROTOCOL.md`、`MODULES.md`、`PROVIDER.md`、`ROADMAP.md`。变更靠 git 追踪；每份顶部标注 `状态` 与对齐的决策编号。
- **账本文档**（只追加，永不删改）：`DECISIONS.md`（重大决策）、`PROGRESS.md`（进度日志）。

**任何"改变架构约定"的变更，必须三件事一起做：① 改活文档 ② DECISIONS.md 追加一条 ③ 一个清晰 git commit。** 琐碎修改只需 commit。

---

## 四、不可违背的架构铁律（来自 ARCHITECTURE.md）

1. **内核不认识任何具体模块。** 若"为加某模块必须改内核"，说明钩子点漏了——补钩子，而非让内核认识模块。
2. **内核极简。** 想往内核加东西前先问"这能不能做成模块？"
3. **一切能力皆模块**，可插拔、可隐藏、缺失时优雅降级。
4. **消息协议与厂商无关**，厂商差异只在 provider 层。
5. **模块只通过统一的 `Context` 对象读写数据**，不得私下互传参数、不得绕过钩子改主循环。
6. **拦截者必须有明确优先级和"改了什么"的契约。**
7. **内核同步实现**（决策 #1），并行/流式用生成器或线程池，暂不引入 async。

---

## 五、工程基线（生产级的含义）
- 类型标注（type hints）覆盖
- 错误处理：工具/模型出错要能捕获并转成可喂回模型的消息，不能让程序崩
- 每个模块可独立测试；每层留一个能手动跑的 demo（`examples/`）
- 每个 commit message 清晰说明"改了什么、为什么"

---

## 五·补 Git 使用规则（决策 #6）

**远程仓库**：`https://github.com/2358447/Modularization-agent`（origin）。

1. **提交时机**：一个逻辑完整的改动 = 一个 commit（能用一句话说清）。触发点：一个模块/子功能完成、一份文档写完、一个 bug 修好、一次重构完成。**不把跑不起来的代码提交到 main**（半成品走功能分支）。
2. **commit message 格式**：`类型: 简述`。类型用 `feat`/`fix`/`docs`/`refactor`/`test`/`chore`。简述用中文说清"做了什么"，需要时空一行补"为什么"。
3. **分支策略（A：main + 功能分支）**：
   - `main` **永远保持可运行**，是当前稳定真相。
   - 每个新模块/大改动开功能分支：`feat/<名字>`（如 `feat/hooks-kernel`）。做完 + owner 审过 + 验收通过，才合并回 main。
   - main 的合并点天然成为"章节里程碑"，服务于将来开源的可讲述性。
4. **push**：`main` 有实质更新即推送到 origin。**推送属于对外发布，若涉及新的外部目标需 owner 授权**；推 origin/main 已获授权。
5. **铁律**：API key/密码一律走环境变量或 `.env`（已被 .gitignore 忽略），**任何情况下不写进代码或文档提交**。

---

## 六、当前进度指针

**详见 `docs/PROGRESS.md`**（每次工作结束都要更新它）。
一句话现状：**处于立项文档阶段，架构总纲已定，正在编写各细化设计文档，尚未开始写内核代码。**

---

## 七、文档地图

| 文件 | 作用 | 类型 |
|------|------|------|
| `CLAUDE.md` | 本文件，协作契约+铁律+进度指针 | 活 |
| `docs/ARCHITECTURE.md` | 架构总纲（锚文档，先读） | 活 |
| `docs/DECISIONS.md` | 重大决策日志 | 账本·只追加 |
| `docs/PROGRESS.md` | 进度日志：现在在哪·下一步 | 账本·只追加 |
| `docs/HOOKS.md` | 钩子点权威语义（待写） | 活 |
| `docs/MESSAGE_PROTOCOL.md` | 消息数据结构（待写） | 活 |
| `docs/MODULES.md` | 模块总目录+模板+优先级（待写） | 活 |
| `docs/PROVIDER.md` | provider 抽象接口（待写） | 活 |
| `docs/ROADMAP.md` | 实现顺序+验收标准（待写） | 活 |
