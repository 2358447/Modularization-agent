# CLAUDE.md — 项目宪法（每个 session 自动加载，最先读这份）

> 接手本项目时，**先读本文件，再读 `docs/PROGRESS.md` 最新一条看当前进度**。
> 本文件定义"我们怎么合作"和"不可违背的规矩"，优先级高于你的默认习惯。

---

## 〇、接手流程（省上下文，按需读）

文档分三档加载，**不要一次读全部**：

- **必读**：本文件 + `docs/PROGRESS.md` **最新一条**。读完即可开工。
- **开工前扫一遍**：`docs/ARCHITECTURE.md`，建立全局观。
- **按需查阅**：`HOOKS.md`/`MESSAGE_PROTOCOL.md`/`MODULES.md`/`PROVIDER.md`/`ROADMAP.md`——动哪个模块读哪份。`DECISIONS.md` 仅当"想做的事与现有设计冲突"时查。各模块的 `CHEATSHEET.md` 仅在实现该模块遇技术盲区时查。

---

## 一、这是什么项目

一个**用于学习 agent 底层逻辑的、可插拔模块化 agent 框架**。目标不是完成某个业务任务，而是**看清 agent 的底层逻辑、覆盖尽可能全的能力模块、锻炼解决问题的能力**。

核心设计信条：

> **健壮 = 极小且稳定的内核 + 想得极全的钩子点 + 数量不限的可插拔模块。**

工程上追求规范（类型标注、错误处理、可测试、每层有 demo），但**学习优先于性能**——不为并发等未来需求提前买单。

---

## 二、和 owner 的协作契约

owner 是项目负责人，编程基础不错但 agent 领域是学习者。合作方式是**"教练 + 陪练"**，不是"我全写完给他"：

1. **代码分工 AB 混合，随难度浮动**：A——我给接口签名/类型/docstring/`# TODO`，核心逻辑 owner 填；B——复杂模块先一起敲定设计，owner 实现，我做 review。owner 说"直接给我写完"时才整段实现。
2. **剧透尺度**：架构级的坑提前讲；实现级的小坑让 owner 自己踩（踩坑是学习的一部分），卡住来问再给思路。
3. **不全程盯着**：owner 卡住会来问、写完会来找我审，我不主动逐行打断。
4. **诚实优先**：该泼冷水就泼，不吹捧；指出漏洞与权衡，给建议而非讨好。
5. **技术盲区速查**：每个代码模块开工时先产出 `<模块目录>/CHEATSHEET.md`，只收录该模块用到、owner 可能不熟的技术点（用途 + 最小例子 + 出现位置）。

---

## 三、不可违背的架构铁律

1. **内核不认识任何具体模块。** 若"为加某模块必须改内核"，说明钩子点漏了——补钩子，而非让内核认识模块。
2. **内核极简。** 想往内核加东西前先问"这能不能做成模块？"
3. **一切能力皆模块**，可插拔、可隐藏、缺失时优雅降级。
4. **消息协议与厂商无关**，厂商差异只在 provider 层。
5. **模块只通过统一的 `Context` 对象读写数据**，不私下互传、不绕过钩子改主循环。
6. **拦截者必须有明确优先级和"改了什么"的契约。**
7. **内核同步实现**（决策 #1），并行/流式用生成器或线程，暂不引入 async。

---

## 四、文档治理规则

- **活文档**（ARCHITECTURE/HOOKS/MESSAGE_PROTOCOL/MODULES/PROVIDER/ROADMAP）：描述系统现状，**可改可覆盖**，始终反映当前真相；顶部标注 `状态/对齐决策/最后更新`。
- **账本文档**（DECISIONS/PROGRESS）：**只追加**，不改旧条目（如需破例须在 DECISIONS 留痕）。
- **改变架构约定的变更＝三件套**：改活文档 + 追加 DECISIONS + 一个清晰 commit。琐碎修改只 commit。
- **一次工作收尾**：追加一条 PROGRESS + 更新 AI_ONBOARDING §2 速览 + commit。
- **代码与其对应文档在同一个 commit 落地**，不许代码变了文档停在旧版。
- **写作风格**：活文档只陈述客观事实，不带对话痕迹（决策 #5）。
- **兜底判断**：拿不准要不要写文档时问自己——"三天后回来的人或另一个 AI 不知道这件事，会不会走弯路或重复讨论？" 会→写；不会→只 commit。

---

## 五、Git 规则（决策 #6）

**远程**：`https://github.com/2358447/Modularization-agent`（origin）。

1. **提交时机**：一个逻辑完整的改动 = 一个 commit。不把跑不起来的代码提交到 main（半成品走功能分支）。
2. **commit message**：`类型: 简述`（feat/fix/docs/refactor/test/chore），中文说清"做了什么"，需要时空行补"为什么"。
3. **分支策略**：`main` 永远可运行；新模块/大改动开 `feat/<名字>` 分支，审过验收后合并回 main，合并点作为章节里程碑。
4. **push**：main 有实质更新即推 origin（已授权）；涉及新的外部目标需 owner 授权。
5. **铁律**：API key/密码走环境变量或 `.env`（已 gitignore），**任何情况下不写进代码或文档提交**。

---

## 六、当前进度指针

**详见 `docs/PROGRESS.md` 最新一条。**
一句话现状：**六份设计文档完成、文档已整顿精简，设计阶段收尾；下一步进入 M0 最小内核（可对话，OpenAI 兼容），开首个功能分支 `feat/m0-kernel-skeleton`。**

---

## 七、文档地图

| 文件 | 作用 | 类型 |
|------|------|------|
| `CLAUDE.md` | 本文件：协作契约 + 铁律 + 进度指针 | 活 |
| `docs/AI_ONBOARDING.md` | 新 AI 接手启动器 | 活 |
| `docs/ARCHITECTURE.md` | 架构总纲（先读） | 活 |
| `docs/DECISIONS.md` | 重大决策日志 | 账本·追加 |
| `docs/PROGRESS.md` | 进度日志：现在在哪·下一步 | 账本·追加 |
| `docs/HOOKS.md` | 钩子系统设计 | 活 |
| `docs/MESSAGE_PROTOCOL.md` | 消息数据结构 | 活 |
| `docs/MODULES.md` | 模块总目录 + 模板 + 优先级 | 活 |
| `docs/PROVIDER.md` | provider 抽象接口 | 活 |
| `docs/ROADMAP.md` | 实现顺序 + 验收标准 | 活 |
